<?xml version="1.0" encoding="UTF-8"?>
<!-- Repository and Service layers -->
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:jpa="http://www.springframework.org/schema/data/jpa" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:security="http://www.springframework.org/schema/security"  xmlns:integration="http://www.springframework.org/schema/integration"
	xmlns:int-jme="http://www.springframework.org/schema/integration" xmlns:int-jms="http://www.springframework.org/schema/integration/jms"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/jms http://www.springframework.org/schema/integration/jms/spring-integration-jms.xsd">

	<!-- Enable @Controller annotation support -->
	<!-- <mvc:annotation-driven /> -->

	<mvc:annotation-driven>
		<mvc:message-converters register-defaults="true">
			<bean
				class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
				<property name="objectMapper">
					<bean class="com.neuralnoise.util.CustomObjectMapper"></bean>
				</property>
			</bean>
		</mvc:message-converters>
	</mvc:annotation-driven>

	<!-- Scan classpath for annotations (eg: @Service, @Repository etc) -->

	<!-- Integration -->
	<context:component-scan base-package="com.neuralnoise.messaging" />

	<!-- MAP -->
	<context:component-scan base-package="com.neuralnoise.map" />

    <!-- Define a channel to communicate out to a JMS Destination -->
    <integration:channel id="topicChannel"/>
    
        <!-- Define an adaptor that route topicChannel messages to the myTopic topic -->
    <int-jms:outbound-channel-adapter connection-factory="jmsFactory" channel="topicChannel" destination-name="topic.myTopic" pub-sub-domain="true" />

    <!-- Define the ActiveMQ connection factory -->
    <bean id="jmsFactory" class="org.apache.activemq.spring.ActiveMQConnectionFactory">
    	<property name="brokerURL" value="tcp://localhost:61616"/>
    </bean>

	<bean class="com.neuralnoise.integration.HelloServiceImpl" name="helloServiceImpl"></bean>

	<!-- A Spring Integration adapter that routes messages sent to the helloWorldChannel to the bean named "helloServiceImpl"'s hello() method -->
	<integration:service-activator input-channel="topicChannel" ref="helloServiceImpl" method="hello" />

	<context:annotation-config />
	
	<!-- enables scanning for @Transactional annotations -->
	<tx:annotation-driven transaction-manager="transactionManager" />
	
</beans>
